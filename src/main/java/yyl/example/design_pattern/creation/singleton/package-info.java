/**
 * 单例模式(Singleton Pattern)：确保某一个类只有一个实例，而且自行实例化并向整个系统提供这个实例。<br>
 * 1、单例类只能有一个实例。 <br>
 * 2、单例类必须自己创建自己的唯一实例。 <br>
 * 3、单例类必须给所有其他对象提供这一实例。<br>
 */
package yyl.example.design_pattern.creation.singleton;
